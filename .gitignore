# ── secrets — never leave the machine ──
.api_token
brain_config.json

# ── private captures & runtime logs ──
cortex_shots/
skeleton_snapshots/
_research/*.log
_research/*.jpg
_research/*.png
temp/

# ── python cruft ──
__pycache__/
*.pyc

# ── quarantined foreign repos (analysis only, never pushed) ──
_xrefs/
scratch/

# ── rust build artifacts (all variants) ──
rust/target/
rust/h264core-py/target/
rust/h264core/target/

# ── runtime telemetry ──
cortex/tool_stats.json

_operator_vault/
